import type { Metadata } from "next";
import { Raleway } from "next/font/google";
import { Header, Footer } from "@/app/_components";
import { footerPayload, payloadPrimary } from "./_lib/data";
import "@/app/_styles/globals.scss";

// Importar las fuentes con las variantes que desees
const raleway = Raleway({
  subsets: ["latin"],
  weight: ["400", "700"],
  display: "swap", // Opcional, para mejorar la carga de la fuente
});

export const metadata: Metadata = {
  title: "Supreme BARF Canine | Alimento superior para perro",
  description: "Generated by create next app",
  icons: {
    icon: "/favicon.ico",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${raleway.className}`}>
        <Header
          data={payloadPrimary.data}
          frontPageID={payloadPrimary.frontPageID}
          elevation={2}
        />
        {children}

        <Footer
          footerMenuDataPrimary={footerPayload.footerMenuDataPrimary}
          footerMenuDataSecondary={footerPayload.footerMenuDataSecondary}
          frontPageID={footerPayload.frontPageID}
          options={footerPayload.options}
        />
      </body>
    </html>
  );
}
